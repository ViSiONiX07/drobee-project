# Bee-Brain Test Suite

Directory structure for automated test suites:

- `integration/`: End-to-end integration tests for Python telemetry pipelines, SQLite persistence, and schema validation.
- `test_data/`: Sample telemetry captures and mock point cloud datasets.

## Running Tests

Run pytest from the repository root:

```bash
pytest
```
